// страница стены
import Post from './Post';

type Props = {
   id?: number,
   message?: string,
   likescount?: number,
   img?: string
}

const WallPage: React.FC<Props> =
   ({ id, message, likescount, img }) => {


      return (
         <>
            
         </>
      );
   }

export default WallPage;